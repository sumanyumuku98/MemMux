[package]
name = "memmux-metrics"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Cross-platform process accounting and task attribution for MemMux (Phase 0)."

[dependencies]
memmux-core = { workspace = true }
serde = { workspace = true }
thiserror = { workspace = true }

# libc is used by the macOS sampler and by recursive process-tree termination (all Unix).
[target.'cfg(unix)'.dependencies]
libc = { workspace = true }

[dev-dependencies]
serde_json = { workspace = true }
